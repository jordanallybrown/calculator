import CalculatorViewer from "./view.mjs";

// once all of the DOM has loaded on the page
window.addEventListener('DOMContentLoaded', function() {
    const calculatorViewer = new CalculatorViewer();

});